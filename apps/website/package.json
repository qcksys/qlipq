{
  "name": "qlipq-website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "sync": "astro sync",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy:dev": "astro build && wrangler deploy --env dev",
    "deploy:prod": "astro build && wrangler deploy --env production"
  },
  "dependencies": {
    "@qcksys/qlipq-obs-script": "workspace:*",
    "astro": "^5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9",
    "typescript": "catalog:",
    "wrangler": "^4"
  }
}
