# Rust / Cargo build output
/target

# Machine-specific build config (absolute paths to the local FFmpeg SDK / libclang for libav-preview)
/.cargo/config.toml

# Backup files from `cargo fmt` / rustfmt
**/*.rs.bk

# MSVC debug info
*.pdb
